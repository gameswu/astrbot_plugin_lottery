{
    "enable_create_notification": {
        "type": "bool",
        "default": true,
        "description": "是否启用创建抽奖广播"
    },
    "enable_result_notification": {
        "type": "bool",
        "default": true,
        "description": "是否启用用户中奖广播"
    },
    "enable_draw_notification": {
        "type": "bool",
        "default": true,
        "description": "是否启用最终结果广播"
    }
}